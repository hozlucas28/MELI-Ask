{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Build image",
            "type": "dockerfile",
            "request": "launch",
            "dockerfile": "Dockerfile",
            "contextPath": "${workspaceFolder}"
        }
    ]
}
