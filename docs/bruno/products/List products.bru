meta {
  name: List products
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/products
  body: none
  auth: none
}

headers {
  Accept: application/json
}

tests {
  test("should return products", function() {
    expect(res.status).to.equal(200);
    expect(res.body).to.be.an("array");
  });
}

docs {
  Retrieves the complete in-memory product catalog.
}
